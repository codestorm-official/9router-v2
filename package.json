{
  "name": "9router-v3",
  "private": true,
  "version": "3.0.0",
  "description": "9Router V3 by codestorm — Railway-ready AI gateway monorepo",
  "workspaces": [
    "frontend",
    "backend",
    "backend/open-sse"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=9router-backend\" \"npm run dev --workspace=9router-frontend\"",
    "build": "npm run build --workspace=9router-backend && npm run build --workspace=9router-frontend",
    "backend": "npm run dev --workspace=9router-backend",
    "frontend": "npm run dev --workspace=9router-frontend",
    "start": "npm run start --workspace=9router-backend",
    "typecheck": "npm run typecheck --workspace=9router-backend && npm run typecheck --workspace=9router-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
